# Accessibility Report

The More Map Actions control preserves an accessible name and `aria-controls`, updates `aria-expanded`, supports Escape and arrow navigation, restores focus when dismissed by keyboard, exposes menu/menuitem roles, and uses visible focus outlines. Reduced-motion preferences disable the menu animation. Physical VoiceOver validation remains pending.
