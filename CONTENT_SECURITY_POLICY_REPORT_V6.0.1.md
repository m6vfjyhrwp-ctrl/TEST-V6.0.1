# Content Security Policy Report

The HTML meta policy restricts default, script, style, connection, object, base, and form sources to the local application and allows only required local/data/blob images. Meta CSP cannot provide every header-level protection; in particular, `frame-ancestors` is not reliably enforced from meta delivery. Hosting-level headers remain recommended where available.
